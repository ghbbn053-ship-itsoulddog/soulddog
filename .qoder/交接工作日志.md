# 交接工作日志（AI接任用）

更新时间：2026-04-18  
维护者：Codex（当前轮）

## 1. 项目现状
- 目标：教务系统 AI 助手（登录教务、抓取数据、RAG/工具调用、聊天流式输出），后续要 MCP 化与简历项目化。
- 当前阶段：内核稳定化 + 开发/生产双部署方案已落地。

## 2. 关键架构（当前）
- 后端入口：`backend/main.py`（装配层）
- 路由拆分：
  - 认证与同步：`backend/app/api/auth_sync.py`
  - 教务查询：`backend/app/api/education.py`
  - 选项接口：`backend/app/api/options.py`
  - 聊天：`backend/app/api/chat.py`
  - MCP HTTP：`backend/app/api/mcp.py`
- 运行时与配置：
  - `backend/app/core/runtime.py`
  - `backend/app/core/config.py`
- 会话/安全：
  - `backend/app/services/session_store.py`
  - `backend/app/security.py`
  - `auth_session_id`（服务端会话强绑定 username）

## 3. 已完成工作（重点）
- 聊天流式稳定性修复（SSE解析、done+content同帧、失败兜底非流式）
- 前端闪烁优化（流式阶段纯文本渲染 + 节流刷新）
- 会话隔离升级：从前端学号参数校验提升到服务端 `auth_session_id` 绑定校验
- 后端 `main.py` 巨石拆分为模块路由
- 增加基础测试：`backend/tests/*`
- 新增生产部署方案（网关代理 + SSE禁缓冲）

## 4. 最近关键提交（按时间）
- `4082ce7` fix(security): handle request without app scope in isolation guard
- `e8d3f3e` feat(security): enforce server-side auth session binding for username isolation
- `44f4e83` feat(deploy): add production compose and gateway proxy with sse-safe config
- `7c5be9f` fix(stream): bypass next proxy by direct backend url for chat stream/send
- `f6c2b24` fix(chat): fallback to non-stream response when stream fails or returns empty
- `c73c344` fix(ui): throttle stream chunk rendering to reduce periodic flicker

## 5. 开发/生产运行方式
### 开发（热更新）
```bash
docker compose -f docker-compose.yml up -d --build
docker compose -f docker-compose.yml logs -f frontend backend
```
登录地址：`http://<server-ip>:5000/login`

### 生产（非热更新）
```bash
docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build
docker compose -f docker-compose.prod.yml logs -f gateway backend frontend
```
登录地址：`http://<server-ip>/login`

## 6. 已知问题（仍需持续优化）
- 聊天页仍有轻微周期性闪烁（已显著降低，但未完全归零）
- 教务查询工具调用偶发超时/断流时，前端虽然有兜底，但体验仍可能抖动
- RAG 命中质量还未做第二阶段硬过滤（按 user_id + data_type + semester）

## 7. 下一阶段计划（接任者直接做）
1. RAG精准化（二阶段）
   - 向量检索强过滤：`user_id + data_type + semester`
   - 降低“牛头不对马嘴”
2. 统一可观测性
   - `send-stream` 添加 trace_id、首token耗时、fallback计数
3. MCP产品化
   - 提供标准工具schema、鉴权、限流、部署说明

## 8. 验收建议（最少）
1. A学号登录后改请求为B学号，必须被401/403拒绝
2. “我的课表大二下”连续10次：不能只留问题无回答
3. 登录页验证码请求必须命中 `/api/captcha`（不能变成 `/api/api/captcha`）
4. 开发模式和生产模式地址分别可正常登录

## 9. 注意事项（避免回归）
- 不要把 `NEXT_PUBLIC_API_URL=/api` 与前端拼接逻辑弄成 `/api/api/*`
- 生产不要使用 Next dev 代理做 SSE 主链路
- 用户经常更换 API Key；任何接任者改配置时优先看 `.env.prod` 与 compose 环境变量

## 10. 日志续写规范（给下一任）
每次改动后追加：
- 时间
- 目标
- 修改文件
- commit id
- 验证命令与结果
- 遗留问题

---

## 11. 2026-04-21 平台重规划（基于 PLATFORM_UPGRADE_GUIDE）
- 背景：用户要求按 `PLATFORM_UPGRADE_GUIDE.md` 重新规划，走长期路线而非临时修补。
- 本次产出（文档落地）：
  - `docs/PLATFORM_REBUILD_PLAN.md`
  - `docs/PHASE_A_TASKBOARD.md`
- 规划结论：
  - 分四期推进（A内核抽象 / B技能系统 / C多Agent / D生态）
  - 先做 Phase A（模型层统一 + MCP注册中心最小版），确保不破坏现网。
- 接任者下一步：
  - 直接按 `docs/PHASE_A_TASKBOARD.md` 执行 A1→A4
  - 每个任务完成后在本日志追加 commit 与验证结果

## 12. 2026-04-21 平台化改造执行记录（Phase A 扩展）
- 目标：按“拿来主义”推进平台内核，完成可落地能力，不停留在规划文档。
- 本轮新增：
  - 统一模型层：`backend/app/services/model_provider.py`
  - MCP注册中心：`backend/app/services/mcp_registry.py`
  - 模型偏好API：`backend/app/api/models.py`
  - Skill管理器：`backend/app/services/skill_manager.py`
  - Skill管理API：`backend/app/api/skills.py`
  - 前端模型设置页：`frontend/src/app/settings/models/page.tsx`
  - 前端技能管理页：`frontend/src/app/skills/page.tsx`
- 改造接入：
  - `backend/app/api/chat.py` 已切统一模型层
  - `backend/app/api/mcp.py` 已切注册中心
  - `backend/main.py` 已注册 models/skills 路由
  - `frontend/src/app/chat/page.tsx` 已增加“模型/Skills”入口
  - `frontend/src/middleware.ts` 已纳入 `/skills` 与 `/settings` 保护范围
- 依赖：
  - `backend/requirements.txt` 新增 `litellm`、`PyYAML`
- 验证建议（Linux）：
  - `git pull origin main`
  - `docker compose -f docker-compose.yml restart backend frontend`
  - 打开 `/settings/models`、`/skills` 验证可用

### 追加（本轮补全）
- 已把模型切换从“全局环境变量”改为“按用户偏好动态实例化”，避免多用户互相污染。
- 新接口：
  - `GET /api/models/available`
  - `GET /api/models/preference/{username}`
  - `POST /api/models/preference`
  - `GET /api/skills/{username}`
  - `POST /api/skills/upload`
  - `POST /api/skills/{skill_name}/enable`
  - `DELETE /api/skills/{skill_name}`
- 新页面：
  - `/settings/models`
  - `/skills`

## 13. 2026-04-21 平台内核续建（无需停机重构）
- 目标：继续按 `PLATFORM_UPGRADE_GUIDE.md` 推进可扩展内核，不做临时补丁。
- 本轮新增（已改代码）：
  - Skill URL 导入端到端打通：
    - 后端接口：`POST /api/skills/import-url`
    - 前端页面：`/skills` 新增 GitHub URL 导入输入与按钮
  - SkillManager 增强：
    - 支持 `github.com/.../blob/...` 自动转 raw URL
    - 增加导入安全约束：
      - 仅允许 `github.com` / `raw.githubusercontent.com`
      - 超时控制
      - 文件大小上限 256KB
  - Chat 路由增强（最小非破坏接入）：
    - 从已启用 skills 中读取 `triggers`
    - 命中时构建“技能路由提示”注入到模型上下文
    - 同步适配非流式与流式路径
- 修改文件：
  - `backend/app/services/skill_manager.py`
  - `backend/app/api/skills.py`
  - `frontend/src/app/skills/page.tsx`
  - `backend/app/api/chat.py`
  - `docs/PHASE_A_TASKBOARD.md`
- 说明：
  - 本轮先实现“可用 + 可维护”的最小能力，不改现有 `/api/chat/*` 返回协议；
  - 后续可在 Phase B 继续做 Skill schema 严格校验、签名与市场索引。

## 14. 2026-04-21 结构化内核继续推进（服务层脱耦）
- 目标：减少 API 层耦合，为后续 MCP/多Agent 编排预留复用点。
- 本轮新增：
  - `backend/app/services/skill_router.py`
    - `match_enabled_skills(owner, question)`
    - `build_skill_prompt_hint(owner, question)`
  - `backend/app/api/chat.py`
    - Skill 触发逻辑改为调用 `skill_router`（从 API 文件剥离）
  - `backend/app/services/skill_manager.py`
    - 增加 `validate_skill_yaml`（统一 YAML 解析与校验）
  - `backend/app/api/skills.py`
    - 新增 `POST /api/skills/validate`
  - `frontend/src/app/skills/page.tsx`
    - 上传前先调用 `validate`，避免坏 YAML 直接入库
- 修改文件：
  - `backend/app/services/skill_router.py`（新增）
  - `backend/app/api/chat.py`
  - `backend/app/services/skill_manager.py`
  - `backend/app/api/skills.py`
  - `frontend/src/app/skills/page.tsx`
  - `docs/PHASE_A_TASKBOARD.md`

## 15. 2026-04-21 引入高星新框架运行层（可回退，不破坏主链路）
- 用户要求：优先使用 GitHub 上高星、较新的开源框架直接融入。
- 本轮新增：
  - `backend/app/services/agent_runtime.py`（新增）
  - `backend/app/api/agents.py`（新增）
  - `backend/main.py` 注册 `agents` 路由
  - `backend/requirements.txt` 新增 `openai-agents`
- 新 API：
  - `GET /api/agents/frameworks`
  - `POST /api/agents/run`
- 设计说明：
  - 默认优先 `openai_agents`；
  - 当 SDK 不可用/密钥缺失时自动回退到现有统一模型层；
  - `langgraph` 先保留兼容入口，下一阶段再接图编排执行器。

## 16. 2026-04-21 自动化“拿来主义”流水线
- 目标：从“手工找仓库”升级为“自动扫描需求 -> 搜索GitHub -> 排名 -> 克隆 -> 报告”。
- 新增：
  - `scripts/github_autopilot.py`
  - `docs/github-intake/AUTOPILOT.md`
  - `backend/app/api/intake.py`
- 新接口：
  - `POST /api/intake/run`（触发扫描/分析/可选克隆）
  - `GET /api/intake/report`（读取最新报告）
- 优化点：
  - 评分加入体量惩罚（过大仓库降权）
  - 相关性过滤（剔除与AI Agent平台无关仓库）
  - 自动更新 `docs/github-intake/repos.txt` 供后续批量拉取

## 17. 2026-04-21 开源能力融入（向量层）
- 来源：自动扫描后优先选取 `txtai` 方向能力做低耦合接入。
- 本轮改造：
  - `backend/app/services/vector_store.py` 新增 `TxtaiVectorStore`
  - 通过 `VECTOR_BACKEND=txtai` 切换后端（默认仍 Milvus）
  - 接口保持兼容：`add_documents/search/delete_user_data`
- 价值：
  - 本地开发无需强依赖 Milvus
  - 保持业务层调用无感切换，利于后续 A/B 对比检索质量

## 18. 2026-04-21 开源能力融入（MCP接入层）
- 目标：让社区 MCP 能力可以“声明即接入”，减少硬编码改造成本。
- 本轮改造：
  - `backend/app/services/mcp_registry.py` 增加外部工具配置加载
  - 新增 `backend/app/mcp/external_tools.json` 示例配置
- 现状：
  - 内置工具 + 外部声明工具统一出现在 `/api/mcp/tools`
  - 保持原有 `call_tool` 路径不变，兼容既有接口

## 19. 2026-04-21 自动化增强（MCP健康探测）
- 新增：
  - `scripts/probe_mcp_external_tools.py`
  - `POST /api/intake/probe-mcp-tools?auto_enable=true|false`
- 能力：
  - 对 `external_tools.generated.json` 中 HTTP 工具执行可达性探测
  - 可选择自动启用可达工具（`enabled=true`）
- 价值：
  - 将“模板配置”推进为“可自动筛选可用工具”，减少手工维护成本

## 20. 2026-04-21 自动化增强（端点线索回填）
- 新增：
  - `scripts/enrich_mcp_external_tools.py`

## 21. 2026-04-24 教务数据审计收口（继续）
- 背景：用户提供 `数据验证3.txt`，已确认一次真实强制同步成功完成。
- 本轮确认：
  - 同步链路正常：PostgreSQL 落库成功、Milvus 向量化成功、快照状态 completed。
  - `personal_info` 已修正：
    - `major=计算机科学与技术`
    - `class=2024计算机科学与技术1班`
  - 剩余问题集中在解析器：
    - `training_plan_count` 仍只有 `13`

## 22. 2026-04-24 平台升级继续（工作区知识库接入层增强）
- 目标：把工作区知识库从“纯文本上传”提升到可承接真实办公文档，补基础数据治理，避免平台层继续积累脏知识。
- 本轮修改：
  - `backend/app/services/workspace_knowledge.py`
    - 新增统一上传解析入口 `ingest_uploaded_document`
    - 支持 `pdf/docx/xlsx/xlsm/pptx/csv/json/txt/md/html/yaml/xml/log`
    - 新增内容哈希 `content_hash` 去重
    - 同一逻辑文件名自动生成 `version`
    - 写入 `authority_level/parser/original_file_size/storage_name`
    - `KnowledgeSource.status` 改为 `processing -> ready`
  - `backend/app/api/workspace.py`
    - 上传接口改为走服务层解析，不再在 API 层硬编码 decode
    - 文本导入/文件上传都支持 `authority_level`
  - `backend/requirements.txt`
    - 新增 `pypdf` / `python-docx` / `openpyxl` / `python-pptx`
- 设计说明：
  - 这是平台能力扩建，不改成熟教务同步链路。
  - 当前版本治理先放在元数据层，后续可继续补数据库唯一索引和版本历史 UI。
- 当前阶段性状态：
  - 工作区上传已具备“文档接入 + 轻治理”基础
  - 下一步建议继续做：
    1. 平台运行时消费 Skill/MCP manifest，而不只是工作区摘要
    2. 前端工作区页补 authority/source/version 展示
    3. 文档解析失败状态与错误详情回显

## 23. 2026-04-24 平台升级继续（Agent Runtime 接入平台对象）
- 目标：让 Agent 运行时真正感知当前平台拼装状态，而不是只塞工作区文本片段。
- 本轮修改：
  - `backend/app/services/agent_runtime.py`
    - 新增统一运行时上下文 `_build_runtime_context`
    - 接入 `PlatformRegistryService`
    - 接入 `CompositionManager`
    - 运行时可感知：
      - 当前选中的工作区
      - 当前命中的工作区知识
      - 当前启用的 Skills
      - 当前启用的 MCP 工具
    - 新增 `_render_runtime_context`，统一给 `openai_agents/langgraph/fallback model provider`
- 设计说明：
  - 这一步是平台对象模型开始参与实际推理，不再停留在“只建表/只列接口”。
  - 当前仍是 prompt/context 级接入，后续可继续演进成真正的 tool routing / planner。
- 当前阶段性状态：
  - Skill/MCP/Workspace 已经开始形成统一运行时上下文
  - 下一步建议继续做：
    1. 前端工作区页展示 `authority_level/version/parser/status`
    2. 文档失败状态、重复命中、版本历史可视化
    3. Agent 根据启用 Skill/MCP 做显式工具路由，而不只是注入上下文

## 24. 2026-04-24 平台升级继续（失败态入库 + Agent 预路由）
- 目标：让平台对象开始影响执行路径，而不是只展示对象清单。
- 本轮修改：
  - `backend/app/services/workspace_knowledge.py`
    - 上传解析失败时不再直接丢异常结束
    - 新增失败文档持久化：
      - `KnowledgeSource.status=failed`
      - `KnowledgeDocument.status=failed`
      - 保留原文件、错误信息、版本号、authority
  - `backend/app/services/agent_runtime.py`
    - `run` 改为异步
    - 新增 Skill 命中后的 MCP 预调用
    - 预调用结果写入统一运行时上下文 `tool_results`
    - 当前为“轻预路由”：先调命中的工具，再把结果注入模型上下文
  - `backend/app/api/agents.py`
    - 对齐异步 runtime
  - `frontend/src/app/workspace/page.tsx`
    - 文档失败时显示具体错误原因
- 设计说明：
  - 这一步的价值不是“更像平台”，而是平台对象第一次真正控制执行。
  - 当前仍采用安全路线：预调用结果先作为 grounded context，不直接替代最终模型输出。
- 当前阶段性状态：
  - 工作区文档具备成功/失败双状态闭环
  - Agent 已开始沿 Skill -> MCP 路径做显式预路由
  - 下一步建议继续做：
    1. 把工具参数提取从正则升级为 schema-aware 参数映射
    2. 给 Agent Runtime 返回结构加 `tool_trace`
    3. 工作区页补重复文档提示与版本链展示

## 25. 2026-04-24 平台升级继续（tool_trace + 参数映射 + 版本提示）
- 目标：把“预路由”从黑箱改成可观察，并让知识治理在前端真正可见。
- 本轮修改：
  - `backend/app/services/agent_runtime.py`
    - 新增 `tool_trace`
    - 工具预调用现在会记录：
      - 命中的 skill
      - 调用的 tool
      - 映射后的 params
      - success / failed / empty
      - error
    - 参数提取从单一 `semester` 正则，升级为基于 tool schema 的轻映射
  - `backend/app/services/workspace_knowledge.py`
    - `list_documents` 阶段补充治理元数据：
      - `duplicate_count`
      - `is_duplicate`
      - `latest_version`
      - `is_latest_version`
  - `frontend/src/app/workspace/page.tsx`
    - 文档卡片新增：
      - 旧版本提示
      - 重复内容提示
- 设计说明：
  - 这一步是“平台可运维化”的开始，不再只有结果，没有执行轨迹。
  - 参数映射当前仍是轻实现，下一步可继续做真正的 schema-aware extractor。
- 当前阶段性状态：
  - Agent 调度链已经具备基础 trace 能力
  - 工作区知识治理开始前后端贯通
  - 下一步建议继续做：
    1. 把 `tool_trace` 透出到前端聊天区
    2. 支持按版本链筛选/合并知识源
    3. 给 Skill 增加显式输入 schema，而不是只靠 triggers

## 26. 2026-04-24 平台升级继续（聊天前端接 Agent Runtime + Skill Schema）
- 目标：让用户在主聊天界面直接使用平台化执行链，而不是只在独立 API 中存在。
- 本轮修改：
  - `backend/app/api/chat.py`
    - `ChatRequest` 新增：
      - `execution_mode`
      - `agent_framework`
    - 非流式 `/api/chat/send` 可切换到 `AgentRuntime`
    - `ChatResponse` 新增 `tool_trace`
    - 聊天消息 `message_meta` 落库存储 `tool_trace`
  - `frontend/src/app/chat/page.tsx`
    - 聊天输入区新增：
      - `EXECUTION`
      - `AGENT FRAMEWORK`
    - assistant 消息卡片新增 `Tool Trace` 展示
  - `backend/app/services/skill_manager.py`
    - Skill 校验支持 `input_schema`
  - `backend/app/services/platform_registry.py`
    - 平台 skill manifest 同步 `input_schema`
  - `backend/app/api/platform.py`
    - skill 列表接口回传 `input_schema`
  - `frontend/src/app/skills/page.tsx`
    - 默认示例 YAML 增加 `input_schema`
    - 技能列表显示 schema 摘要与完整 JSON
- 设计说明：
  - 这一步把“平台对象 -> 运行时 -> 用户界面”打通了第一条主链。
  - 当前 Agent Runtime 只接在非流式聊天分支，避免破坏成熟 SSE 主链路；后续再考虑流式 agent。
- 当前阶段性状态：
  - 用户已经可以在聊天主界面切换 `Chat / Agent Runtime`
  - Skill 规范已从“触发词驱动”迈到“触发词 + 输入 schema”
  - 下一步建议继续做：
    1. 把流式聊天也接入 agent/tool_trace
    2. Skill schema 驱动参数抽取，而不仅是 tool schema
    3. 聊天侧补 MCP/Skill 组合状态可视化

## 27. 2026-04-24 平台升级继续（流式聊天接 Agent Runtime）
- 目标：补齐主聊天链路最后一个明显断层，避免“Agent 只能非流式”的半成品体验。
- 本轮修改：
  - `backend/app/api/chat.py`
    - 流式 `/api/chat/send-stream` 新增 `execution_mode=agent` 分支
    - 当切到 Agent Runtime 时：
      - 直接流式分片输出 agent content
      - `done` 帧带回 `tool_trace`
      - assistant 消息 `message_meta` 持久化 `tool_trace`
  - `frontend/src/app/chat/page.tsx`
    - SSE `done` 分支即使没有 `tool_calls` 也会接收 `tool_trace`
    - 聊天历史恢复时同步恢复：
      - `tool_calls`
      - `tool_trace`
- 设计说明：
  - 当前仍然是“agent content 伪流式切片输出”，不是 agent 内部真实 token 流，但已经打通前后端体验。
  - 这是可接受的阶段性方案，后续再替换成真正的 agent streaming runner。
- 当前阶段性状态：
  - `Chat / Agent Runtime` 现在都支持主聊天页流式展示
  - `tool_trace` 已贯通：执行 -> SSE -> 持久化 -> 历史恢复 -> UI 展示
  - 下一步建议继续做：
    1. MCP/Skill 当前组合状态在聊天页顶部显式展示
    2. Skill schema 驱动更细的参数抽取器
    3. Agent Runtime 引入 planner / multi-step tool orchestration

## 28. 2026-04-24 平台升级继续（聊天页组合状态可视化）
- 目标：让用户在主聊天页直接看到当前拼装状态，减少“我现在到底启用了什么”的不可见性。
- 本轮修改：
  - `frontend/src/app/chat/page.tsx`
    - 启动时同步拉取 `/api/composition/{username}`
    - 聊天顶部新增轻量状态条，显示：
      - 当前 workspace
      - 当前 execution 模式
      - 当前启用 skill 数量与预览
      - 当前启用 mcp 数量与预览
- 设计说明：
  - 这是用户视角的必要补强，不涉及后端协议变化。
  - 当前采用“轻状态条”，后续可升级成可展开的 composition inspector。
    - `academic_progress` 仍只有首模块/列位风险
- 本轮修改：
  - `backend/scraper.py`
    - 新增 `_expand_table_rows()`，统一展开 `rowspan/colspan`
    - `get_my_training_plan()` 改为基于展开后的规则表解析
    - 课程代码识别从“仅8位数字”放宽到字母数字混合，兼容 `2114008X`
    - `get_academic_progress()` 改为优先动态表头映射，兼容 `课程模块/课程类别/模块应修学分`
- 本地验证：
  - `python -m py_compile backend/scraper.py` 通过
  - 用 `.qoder/教务系统源代码/我的培养方案.txt` 本地展开表后，已能识别 `21` 行课程记录，明显高于之前的 `13`
- 下一步：
  - 推送后让 Linux 只重建 `backend`
  - 再次检查：
    - `training_plan_count`
    - `academic_progress`
    - `backend logs`

### 追加（2026-04-24 第二轮修正）
- 新测试结果：
  - `training_plan_count=21`，说明培养方案解析已明显回正
  - `personal_info` 已稳定正确
  - `academic_progress` 仍只落了 2 门，`总学分要求=0`
- 根因：
  - 学业进度页不是“只解析第一张命中表”就够，现有逻辑只处理了首个模块表
  - 汇总学分应按多张表的 `合计` 行累加，而不是只取第一张表
- 本轮继续修改：
  - `backend/scraper.py`
    - `get_academic_progress()` 改为扫描全部命中表
    - 合并多张表课程行并按 `(课程代码, 课程名称)` 去重
    - 累加每张表 `合计` 的已获学分
    - 对 `需修读总学分` 提取增加空白清洗
- 本地验证：
  - `python -m py_compile backend/scraper.py` 通过
  - `POST /api/intake/enrich-mcp-tools`
- 能力：
  - 从 `vendor/autopilot/<repo>` 的 README/docker-compose 等文件提取 URL/端口线索
  - 自动回填 `external_tools.generated.json` 的 `url`
- 价值：
  - 将“统一占位 URL”升级为“按仓库推断 URL”，提升后续可达探测命中率

## 21. 2026-04-21 阶段性完成：全自动接入流水线
- 新增 API：
  - `POST /api/intake/pipeline`

## 22. 2026-04-24 数据链路审计与清洗（可上线性检查）
- 目标：
  - 用 `.qoder/教务系统源代码` 对照现有教务爬虫真实入口、表单与数据链路；
  - 判断当前爬虫、结构化存储、向量化、分区是否适合上线；
  - 对脏数据/错数据/旧批次串读问题做结构修复。
- 审计结论：
  - 当前代码在本轮修复前，不适合直接按“数据可信”标准上线。
  - 主要风险不是“爬不到”，而是“爬到错页/旧页也会被当成功数据复用”。
- 已确认的关键问题：
  - `auth_sync.py` 之前会复用任意旧 `EducationData`，没有快照新鲜度语义，可能复用脏/旧数据。
  - `education_sync.py` 之前即使存储/向量化失败，也可能对外显示同步完成。
  - 向量库之前只按 `user_id` 维度删后重建，没有 `sync_key/schema_version` 批次语义。
  - 检索时之前没有限定“当前激活快照”，历史脏批次可能混入回答。
  - `get_my_training_plan()` 之前抓错了入口：代码抓的是 `/pyfa/pyfa_query`，而原始页面明确“我的培养方案”是 `/pyfa/pyfazd_query`；`/pyfa/pyfa_query` 实际是“执行计划”。
- 本轮代码修复：
  - `backend/app/models/education_data.py`
    - 新增 `EducationSyncSnapshot` 表，保留每次同步状态、原始数据、标准化结果、摘要、错误信息。
  - `backend/app/services/education_audit.py`
    - 新增同步审计辅助：`sync_key`、标准化摘要、空载荷判断、错误规范化。
  - `backend/app/api/auth_sync.py`
    - 登录后仅复用“最近 12 小时内的成功激活快照”，不再无条件复用旧 `EducationData`。
  - `backend/app/services/education_sync.py`
    - 同步开始时确保用户存在，再创建快照，避免 `user_id=0` 这种伪关联；
    - 只有结构化存储与向量化都成功，才会把快照标为 `success/is_active`。
  - `backend/scraper.py`
    - `get_my_training_plan()` 已改为抓 `/pyfa/pyfazd_query`；
    - 按真实 `id='mxh'` 表格和 `rowspan` 结构解析课程，不再把“执行计划”错当“我的培养方案”。
  - `backend/app/services/data_processor.py`
    - 课表/考试 chunk 的学期元数据统一取外层学期；
    - 每个向量 chunk 追加 `sync_key/schema_version`；
    - 向量化流程改成“先生成并写入新批次，再删除旧批次”，降低失败时清空用户向量的风险。
  - `backend/app/services/vector_store.py`
    - 向量去重从 `(user_id, text)` 提升为 `(user_id, text, sync_key)`；
    - 支持按 `sync_key` 过滤检索；
    - 支持删除用户旧批次但保留当前 `sync_key`。
  - `backend/app/api/chat.py`
    - RAG 检索时只读当前激活成功快照对应的 `sync_key`，避免历史批次串读。
- 仍未完成但必须列入上线前清单：
  - 需要真实登录一次，验证 `pyfazd_query / xskb_list.do / cjcx_list / xyjdcx?type=cx / xsksap_query / grxx/xsxx` 在当前学校环境下都仍可正常返回。
  - 需要检查一次生产库里历史 `education_data` 与向量库是否已有污染批次，必要时做一次全量重建。
  - 需要补充最少集成测试：成功同步、空载荷失败、向量失败回滚语义、激活快照检索命中。
- 接任者下一步：
  1. 在 Linux 环境真实登录一个测试账号，跑完整同步并看快照表内容。
  2. 核验当前 Milvus/txtai 中旧批次是否已被清理。
  3. 若数据正确，再讨论是否允许上线。
- 串联步骤：
  - `run -> generate -> enrich -> probe -> mcp reload`
- 意义：
  - 从“脚本级自动化”升级为“服务级一键自动化”
  - 可直接用于后续定时任务/后台任务编排

## 22. 2026-04-21 下一阶段启动（可观测性）
- 新增：
  - `GET /api/intake/pipeline/history`
  - `GET /api/intake/pipeline/latest`
- 运行记录落盘：
  - `docs/github-intake/pipeline-history.jsonl`
- 记录内容：
  - 启动时间、总耗时、各步骤耗时、参数、reload_count、成功状态

## 23. 2026-04-22 Intake 任务编排闭环（阶段收口）
- 目标：避免“局部最优”脚本化，升级为可追踪、可回滚、可重试的服务化流水线。
- 本轮完成：
  - `backend/app/api/intake.py`
    - 修复失败分支耗时计算（统一使用 `perf_counter`，避免时钟源混用）
    - 任务状态与快照能力保持可用：`pipeline-tasks.json` + `snapshots/<run_id>`
    - 支持 `tasks` 查询、`retry` 重试、`rollback` 回滚、`state/unlock` 状态控制
  - `frontend/src/app/mcp/page.tsx`
    - 新增“任务队列”面板（状态/耗时/error/snapshot）
    - 支持单任务 `重试` 与 `回滚` 按钮
    - 增加 8 秒轮询刷新 `state/history/tasks`，减少手动排查
  - `docs/github-intake/AUTOPILOT.md`
    - 补充 task/state/retry/rollback/unlock API 文档
    - 补充任务与快照文件落盘路径说明
- 意义：
  - 流水线进入“可运维”阶段，不再只能看 backend 日志判断问题；
  - 后续可直接接入定时任务/后台 worker（如 Celery/Arq）而无需推倒重来。

## 24. 2026-04-22 Intake 异步队列化（下一阶段启动）
- 目标：从“API 同步阻塞执行”升级为“后台队列执行”，降低前端等待与超时风险。
- 本轮完成：
  - `backend/app/api/intake.py`
    - `POST /api/intake/pipeline` 改为入队即返回（`queued=true`, `run_id`, `queue_size`）
    - 新增后台 worker 线程，串行消费 `queued` 任务并写回 `running/success/failed`
    - `GET /api/intake/pipeline/state` 增加 `queue_size`
  - `frontend/src/app/mcp/page.tsx`
    - 一键流水线与重试按钮改为“提交任务”语义
    - 状态栏增加队列长度显示
  - `docs/github-intake/AUTOPILOT.md`
    - 补充异步入队模式说明与轮询建议
- 说明：
  - 当前实现为进程内 worker（轻量版），后续可平滑切换到 Redis/Celery/Arq 等独立队列。

## 25. 2026-04-22 Intake 稳定性增强（取消/超时/幂等）
- 目标：把队列执行从“能跑”提升到“可控、可恢复、可防抖”。
- 本轮完成：
  - `backend/app/api/intake.py`
    - `POST /api/intake/pipeline` 新增：
      - `timeout_sec`（任务总超时预算）
      - `idempotency_key`（幂等去重）
    - 新增参数指纹去重（同 owner 在短窗口内重复请求复用现有 queued/running 任务）
    - 新增任务取消接口：`POST /api/intake/pipeline/tasks/{run_id}/cancel`
    - 运行中取消支持：终止当前子进程并将任务置为 `cancelled`
    - 状态接口新增 `running_count`
  - `frontend/src/app/mcp/page.tsx`
    - 任务卡新增“取消”按钮（queued/running 可用）
    - 顶部状态新增运行中数量
    - 一键流水线支持去重反馈（命中去重时提示复用任务）
  - `docs/github-intake/AUTOPILOT.md`
    - 补充 cancel/idempotency/timeout 文档

## 26. 2026-04-22 调度增强（优先级 + 指数退避重试）
- 目标：从“单队列串行”升级到“可调度”的队列执行器，避免低价值任务阻塞关键任务。
- 本轮完成：
  - `backend/app/api/intake.py`
    - pipeline 入参新增：
      - `priority` (`high|normal|low`)
      - `max_retries`
      - `retry_backoff_base_sec`
    - 队列选择器按优先级 + 可执行时间排序（支持 `next_run_at`）
    - 失败时自动指数退避重试（2^n * base 秒），达上限后才标记 failed
  - `frontend/src/app/mcp/page.tsx`
    - 一键流水线新增优先级选择
    - 任务卡显示优先级、重试次数、下次执行时间、last_error
  - `docs/github-intake/AUTOPILOT.md`
    - 补充 priority/retry 参数说明

## 27. 2026-04-22 Openclaw 模式接入（runs.sqlite + 多 worker + 日志流）
- 参考来源：`E:/Openclaw/tasks/runs.sqlite`（拿来主义），按其任务持久化思想迁移。
- 本轮完成：
  - `backend/app/api/intake.py`
    - 新增 SQLite 持久化队列：`backend/data/intake/runs.sqlite`
    - 表结构：`task_runs` + `task_logs`
    - 多 worker 并发（`INTAKE_WORKERS`，默认 2）+ claimed 抢占机制
    - 任务 owner 从会话解析（不再固定 system）并用于任务访问鉴权
    - 实时日志查询与 SSE：
      - `GET /api/intake/pipeline/tasks/{run_id}/logs`
      - `GET /api/intake/pipeline/tasks/{run_id}/logs/stream`
    - 失败分级中的熔断基础（脚本连续失败自动短时熔断）
  - `backend/main.py`
    - 启动时自动初始化 intake db 与 worker
  - `docs/github-intake/AUTOPILOT.md`
    - 补充 Openclaw 对齐说明与日志流接口

## 28. 2026-04-22 Skill + MCP 自由拼接（拿来 Openclaw 配置思路）
- 目标：让用户可配置“启用哪些 skill + 哪些 mcp 工具 + 顺序/权重”，实现自由拼装。
- 本轮完成：
  - `backend/app/services/composition_manager.py`（新增）
    - 每用户组合配置存储：`backend/data/composition/<owner>.json`
    - 能力：技能开关、MCP开关、MCP排序权重、解析生效结果
  - `backend/app/api/composition.py`（新增）
    - `GET /api/composition/{username}`
    - `POST /api/composition/skills`
    - `POST /api/composition/mcp`
    - `POST /api/composition/mcp/reorder`
  - `backend/app/services/skill_router.py`
    - 技能匹配前接入 composition 过滤
  - `backend/app/services/mcp_registry.py`
    - 工具调用前校验 composition 是否允许
  - `backend/main.py`
    - 注册 composition 路由

## 29. 2026-04-22 三项并行收口（闪烁治理 + 组合页 + 观测）
- 目标：按用户要求同时推进 1/2/3，不局部优化。
- 本轮完成：
  - 聊天闪烁治理（前端）
    - `frontend/src/app/chat/page.tsx`
      - `fetchConversations/fetchHistory` 改 `useCallback` 稳定引用
      - 历史加载增加 `isLoading/currentConversationId` 护栏，避免发送中历史回写覆盖
      - 发送流式请求透传 `x-trace-id`
      - 侧边栏新增“组合编排”入口
    - `frontend/next.config.ts`
      - 收紧 `allowedDevOrigins`，去掉通配 `*`，减少 dev HMR 异常重连风险
  - 组合编排页面（前端）
    - 新增 `frontend/src/app/composition/page.tsx`
      - 可视化启停 skill
      - 可视化启停 mcp 工具
      - 支持 mcp 顺序上移/下移
    - `frontend/src/middleware.ts`
      - 加入 `/composition` 保护
  - 生产观测（后端）
    - 新增 `backend/app/core/observability.py`
      - trace_id context
      - Prometheus 指标（HTTP/Chat/Intake）
    - `backend/main.py`
      - 全局 middleware：注入/返回 `x-trace-id`
      - `GET /api/metrics` 指标端点
    - `backend/app/api/chat.py`
      - stream 指标统计 + SSE 返回 trace_id
    - `backend/app/api/intake.py`
      - enqueue/start/finish/queue/running 指标更新
    - `backend/requirements.txt`
      - 新增 `prometheus-client`

## 30. 2026-04-23 模型能力升级 + 闪烁链路排查（本轮最新）
- 时间：2026-04-23 19:52:37
- 目标：
  - 按用户需求补齐“类似豆包”的能力：模型可切换、模式可切换、思考流可显示。
  - 同步修复 MCP 页面工具区按钮溢出与布局杂乱。
  - 排查前端频繁刷新/compiling 现象并给出结论。

### 30.1 已完成代码（本地已通过静态检查）
- 模型配置增强（后端）
  - 文件：`backend/app/api/models.py`
  - 新增/扩展字段：
    - `api_base`、`api_key`、`reasoning_mode`、`show_thinking`
  - `GET /api/models/available` 增加能力标记：
    - `supports_custom_endpoint`
    - `supports_reasoning`
  - `GET /api/models/preference/{username}` 返回脱敏 key：`api_key_masked`

- 会话存储增强（后端）
  - 文件：`backend/app/services/session_store.py`
  - 模型偏好持久化扩展为：
    - `provider/model/api_base/api_key/reasoning_mode/show_thinking`
  - 处理规则：`api_key` 未传时保留旧值，避免误清空。

- 统一模型层增强（后端）
  - 文件：`backend/app/services/model_provider.py`
  - 增加事件流接口：`chat_stream_events(...)`
    - 支持 `{"type":"thinking"}` 与 `{"type":"content"}` 双通道输出。
  - LiteLLM Provider 支持用户级覆盖：
    - `model/api_base/api_key`
  - UnifiedModelProvider 增加用户级覆盖构造参数并串联 fallback。

- 聊天接口增强（后端）
  - 文件：`backend/app/api/chat.py`
  - `ChatRequest` 新增：
    - `override_provider/override_model/override_api_base/override_api_key`
    - `reasoning_mode/show_thinking`
  - `send/send-stream` 支持会话级模型覆盖。
  - SSE 新增 `thinking` 帧输出（模型支持时）。

- 聊天页增强（前端）
  - 文件：`frontend/src/app/chat/page.tsx`
  - 输入区上方新增：
    - Provider 下拉
    - Model 下拉
    - 模式下拉（标准/推理/深度推理）
    - “显示思考流”开关
  - 发送时透传上述参数到 `/api/chat/send` 与 `/api/chat/send-stream`。
  - assistant 消息支持 `thinking` 渲染区。

- 模型设置页增强（前端）
  - 文件：`frontend/src/app/settings/models/page.tsx`
  - 增加可配置项：
    - Base URL
    - API Key（脱敏提示，留空不改）
    - 模式与思考流偏好

### 30.2 本轮 UI 调整（已推送）
- MCP 工具页按钮重排/美化：
  - 提交：`eeb7b00`
  - 文件：`frontend/src/app/mcp/page.tsx`

### 30.3 提交与推送状态（关键）
- 已推送成功：
  - `eeb7b00`（MCP 按钮布局美化）
- 本地提交成功但未推送（网络阻塞）：
  - `1436a7e` `feat(chat): add model mode selector and thinking stream with custom provider config`
- 推送失败症状：
  - `Recv failure: Connection was reset`
  - `Failed to connect to github.com port 443`

### 30.4 校验结果
- 前端类型检查：
  - `pnpm --dir frontend -s exec tsc --noEmit` 通过
- 后端语法检查：
  - `python -m py_compile backend/app/api/models.py backend/app/api/chat.py backend/app/services/model_provider.py backend/app/services/session_store.py` 通过

### 30.5 问题定位结论（频繁刷新/compiling）
- `next dev` 的 `compile: xxms` 本身是请求编译统计，不等于异常。
- 真正“页面闪烁刷新”高概率来自 dev HMR websocket 断连重连（局域网 + docker dev 常见）。
- 已在 `next.config.ts` 做 `allowedDevOrigins` 补充（此前轮次），仍需结合访问 IP 校准。

### 30.6 下一任接手第一步（必须先做）
1. 在网络可达 GitHub 的环境推送：
   - `git push origin main`
2. Linux 机器拉取并重启：
   - `git pull origin main`
   - `docker compose -f docker-compose.yml restart backend frontend`
3. 验证新能力：
   - `/settings/models` 能配置 base_url/key/模式
   - `/chat` 输入框上方出现 provider/model/模式/思考流开关
   - 流式时能看到 thinking 区（仅模型支持时）

### 30.7 风险与注意
- 由于用户机器存在大量 `.qoder/repowiki` 脏改，提交时必须继续使用路径级 `git add -- <files>`，避免误带入。
- `login -> chat` 自动跳转是已登录态行为（cookie 有效），非 bug；需要测试登录页时先清 cookie。

## 31. 流式输出/页面频繁刷新长期治理（本轮）

### 31.1 定位结论
- 后端 SSE 已能返回分片，用户抓包中可以看到多条 `data: {"content": ...}`。
- 前端日志持续出现 `GET /chat 200 ... proxy.ts ...`，后端同时出现 `流式连接被客户端中断`。
- 结论：不是模型完全不流，也不是后端完全不发；主要问题是开发环境下页面被反复重新请求，SSE 连接被浏览器端中断。
- 用户响应头曾出现 `content-encoding: gzip`，这对 `text/event-stream` 不理想，可能导致分片被攒包或延迟显示。

### 31.2 已做改动
- `docker-compose.yml`
  - 前端不再默认强制 `WATCHPACK_POLLING=true`。
  - 改为：
    - `WATCHPACK_POLLING=${WATCHPACK_POLLING:-false}`
    - `CHOKIDAR_USEPOLLING=${CHOKIDAR_USEPOLLING:-false}`
  - 目的：Linux Docker 场景默认走 inotify 热更新，避免轮询监听导致 Next dev 频繁 compiling/刷新。
  - 如果未来在 Windows/WSL/远程挂载中热更新失效，再临时用：
    - `WATCHPACK_POLLING=true CHOKIDAR_USEPOLLING=true docker compose -f docker-compose.yml up -d frontend`

- `backend/app/api/chat.py`
  - SSE 响应头改为：
    - `Cache-Control: no-cache, no-transform`
    - `X-Accel-Buffering: no`
    - `Content-Encoding: identity`
  - 目的：明确禁止中间层压缩/转换 SSE，减少“后端已发但前端不逐字显示”的概率。

### 31.3 Linux 更新建议
```bash
git pull origin main
docker compose -f docker-compose.yml up -d --build backend frontend
docker compose -f docker-compose.yml logs -f frontend backend
```

### 31.4 验证重点
- 浏览器 Network 里 `/api/chat/send-stream` 响应头应尽量看到：
  - `content-type: text/event-stream`
  - `cache-control: no-cache, no-transform`
  - 不应再是 `content-encoding: gzip`
- 发送聊天时，前端日志不应持续刷 `GET /chat 200`。
- 后端不应再频繁出现 `流式连接被客户端中断`。

### 31.5 仍需保留的开发体验原则
- 用户经常改前后端代码，所以不能彻底取消热更新。
- 正确方向不是“永久生产模式开发”，而是：
  - 默认保留热更新；
  - 避免 Docker 轮询热更新过度触发；
  - SSE 走不压缩、不缓冲的稳定通道；
  - 只有在流式联调必须极稳时，再临时启用生产式前端构建验证。

### 31.6 继续定位到的 HMR 风暴根因
- 用户 Network 明确出现连续：
  - `webpack.hot-update.json`
  - `webpack.*.hot-update.js`
  - `chat?_rsc=...`
  - `layout.css/page.css ?v=...`
- 这说明不是业务轮询，而是 Next dev HMR 一直认为文件有变化。
- 根因：`frontend/next.config.ts` 配置了 `distDir: 'dist'`，但 `docker-compose.yml` 只排除了 `/app/.next`，没有排除 `/app/dist`。
- 结果：Next dev 写入 `/app/dist`，文件监听器监听到输出目录变化，又触发 hot update，再写入，再触发，形成热更新自循环。
- 已修复：`docker-compose.yml` 前端 volumes 新增匿名卷：
  - `/app/dist`
- 这次变更只改 compose 挂载，不改 Dockerfile/依赖，不需要重构镜像；需要重建 frontend 容器让 volume 生效。

### 31.7 第二个热更新自循环源
- 用户继续反馈 `Fast Refresh rebuilding/done` 高频反复出现。
- 继续排查后确认：
  - [frontend/tsconfig.json](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\tsconfig.json) 开启了 `incremental: true`
  - 前端根目录存在 [frontend/tsconfig.tsbuildinfo](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\tsconfig.tsbuildinfo)
- 这意味着 TypeScript/Next dev 每次增量编译都会改写 `tsconfig.tsbuildinfo`，容器监听到根目录文件变化，又会继续触发 Fast Refresh。
- 已修复：`docker-compose.yml` 前端 volumes 再新增匿名卷：
  - `/app/tsconfig.tsbuildinfo`
- 到这一步，前端开发态已经把两个最常见的“输出文件反向触发监听”点都隔离了：
  - `/app/dist`
  - `/app/tsconfig.tsbuildinfo`
- 此变更仍然不需要重构镜像，但需要重建 frontend 容器。

### 31.8 长期修正：回归 Next 官方输出约定
- 用户继续提供日志后确认：即使隔离了 `/app/dist` 和 `/app/tsconfig.tsbuildinfo`，`Fast Refresh rebuilding` 仍在循环。
- 这说明问题不该继续靠“一个个排除输出文件”兜，而要改回更稳定的工程配置。
- 已做长期调整：
  - [frontend/next.config.ts](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\next.config.ts)
    - 移除 `distDir: 'dist'`
    - 回归 Next 默认 `.next` 输出目录
  - [frontend/tsconfig.json](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\tsconfig.json)
    - 新增 `tsBuildInfoFile: ".next/cache/tsconfig.tsbuildinfo"`
    - 将增量构建产物放到 `.next/cache`，不再落到项目根目录
    - `exclude` 增加 `.next`
  - [docker-compose.yml](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docker-compose.yml)
    - 移除前两次针对 `/app/dist` 和 `/app/tsconfig.tsbuildinfo` 的临时排除
    - 保留 `/app/.next` 作为统一缓存与输出目录
- 这比前两轮 patch 更长期，因为：
  - 顺着 Next 官方默认行为走
  - 减少 dev server 自己写文件又自己监听的非标准路径
  - 配置更少、后续维护成本更低

### 31.9 这次 Linux 处理方式
- 不需要重构镜像：
  - 没改 Dockerfile、依赖、系统层
- 但需要重建 frontend 容器：
  - 因为 compose 挂载变化了，且旧容器里可能残留之前的输出目录状态
### 31.10 回答风格调整：从“结构化字段堆砌”改为“自然语言复述”
- 用户反馈：虽然已经阻止了编造，但当前“你是谁/哪里上课”这类兜底回答太像字段清单，阅读体验差。
- 这次只调整 [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py) 中 grounded answer 的模板层，不放松事实约束：
  - 身份类问题：改为自然句式复述姓名、学号、专业、班级、学院字段；
  - 地点类问题：改为先给出课表里真实出现过的教室/楼栋示例，再明确说明“当前数据不能确认具体校区”；
  - 保留“不补充导航、签到、WiFi、开放时间等未验证信息”的硬约束。
- 这次没有改爬虫、同步、向量化和模型调用逻辑，只是把已经存在的安全回答链路从“死板输出”改成“可读输出”。
### 31.11 平台层第一阶段落地：工作区知识库闭环
- 这次升级按“平台层重构、教务底座少动”的原则推进：
  - 不重写成熟的教务爬虫/同步/问答链路；
  - 新增平台核心对象模型与工作区知识库能力，作为后续 Skill/MCP/图谱/Agent 的公共底座。
- 新增平台核心数据表：
  - `workspaces`
  - `knowledge_sources`
  - `knowledge_documents`
  - `knowledge_chunks`
  - `knowledge_relations`
- 后端新增 [backend/app/models/platform.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\models\platform.py)，并注册到 [backend/app/models/__init__.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\models\__init__.py)。
- 后端新增 [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)：
  - 默认工作区创建；
  - 文本/文件文档入库；
  - 文档切块；
  - 复用现有 embedding + vector store 做向量化；
  - 轻量关系抽取；
  - 工作区知识检索；
  - 将 Skill/MCP/组合配置映射进工作区图谱视图。
- 后端新增 [backend/app/api/workspace.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\workspace.py)：
  - `GET /api/workspace/{username}`
  - `POST /api/workspace`
  - `GET /api/workspace/{username}/documents`
  - `POST /api/workspace/documents/text`
  - `POST /api/workspace/documents/upload`
  - `POST /api/workspace/search`
  - `GET /api/workspace/{username}/graph/{workspace_id}`
- 前端新增 [frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx)：
  - 工作区列表/创建；
  - 手工整理内容直接入库；
  - 上传文本类文件入库；
  - 文档列表；
  - 知识检索；
  - 关系图谱预览。
- 侧边栏已新增“工作区知识库”入口：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - [frontend/src/middleware.ts](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\middleware.ts)
- 这一步完成后，平台已经具备最小可用的：
  - `Workspace -> 上传/录入 -> 清洗切块 -> 入知识库 -> 检索 -> 图谱化预览`
- 下一阶段应继续补：
  - 文档类型扩展（PDF/DOCX/XLSX/OCR）
  - Skill/MCP manifest 正式入库，不再仅靠文件和运行时扫描
  - 聊天主链路接入工作区知识检索
  - Agent runtime 与工作区知识库打通
### 31.12 平台层第二阶段：工作区知识正式接入聊天 + Skill/MCP 平台对象化
- 本阶段目标不是继续堆页面，而是让平台层真正参与主链路。
- 已完成聊天主链路接入：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - 非流式 `/send`：在教务缓存 RAG 之外，追加工作区知识检索结果；
  - 流式 `/send-stream`：将工作区检索结果拼入 `edu_context`，让流式回答也能使用工作区知识。
- 工作区检索服务已补齐为统一结果契约：
  - [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)
  - 检索结果统一带 `text/content/title/source/score`，兼容现有 `chat_with_rag`。
- 新增 Skill/MCP 平台对象持久化：
  - [backend/app/models/platform.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\models\platform.py)
    - `skill_manifests`
    - `mcp_server_manifests`
  - [backend/app/services/platform_registry.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\platform_registry.py)
    - 将现有 Skill YAML 文件同步到数据库对象；
    - 将现有 MCP registry 工具同步到数据库对象；
    - 保持旧文件机制兼容，不强制迁移。
  - [backend/app/api/platform.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\platform.py)
    - `GET /api/platform/{username}/skills`
    - `GET /api/platform/{username}/mcp`
- [frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx) 已补平台对象视图：
  - 工作区页内可直接查看平台层 Skill manifests；
  - 工作区页内可直接查看平台层 MCP manifests；
  - 不再只是“文档页”，而是开始具备“平台控制台”属性。
- 到这一步，平台层已经从“独立演示页”升级成“参与聊天 + 承载能力对象”的实际底座。
- 下一阶段应继续补：
  - 将工作区/平台对象切换纳入前端全局导航与状态；
  - 扩展文档解析类型（PDF/DOCX/XLSX/OCR）；
  - 把 Agent runtime 与工作区知识、Skill/MCP 平台对象接通；
  - 逐步减少运行时文件扫描，向数据库对象和 manifest 统一收口。
### 31.13 平台层第三阶段：当前工作区进入聊天/Agent 运行态
- 本阶段补的是“状态闭环”，不是再加孤立页面。
- 后端会话层新增工作区偏好存储：
  - [backend/app/services/session_store.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\session_store.py)
  - 新增：
    - `set_user_workspace_preference()`
    - `get_user_workspace_preference()`
- 新增工作区偏好 API：
  - [backend/app/api/workspace_pref.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\workspace_pref.py)
  - `GET /api/workspace-preference/{username}`
  - `POST /api/workspace-preference`
- 聊天主链路不再固定取默认工作区，而是优先读取用户当前选中的工作区：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - `_build_workspace_context()` 已支持 `session_store` 读取当前工作区偏好。
- Agent runtime 已接入当前工作区知识摘要：
  - [backend/app/services/agent_runtime.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\agent_runtime.py)
  - `openai_agents` / `langgraph` / fallback model provider 都会带上当前工作区知识上下文。
- 前端聊天页已接入工作区选择器：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - 聊天输入框上方新增 `WORKSPACE` 选择项；
  - 切换后会写回 `/api/workspace-preference`，后端聊天与 Agent 运行都使用该选择。
- 到这一步，平台层已经具备：
  - `工作区知识 -> 聊天`
  - `工作区知识 -> Agent runtime`
  - `前端工作区选择 -> 后端会话态`
- 下一阶段应继续补：
  - 文档解析扩展（PDF/DOCX/XLSX/OCR）
  - 工作区范围内的来源治理（版本、去重、权威级别）
  - Agent runtime 真正消费 Skill/MCP 平台对象，不只是读取工作区知识摘要。
### 31.14 工作区前端按文档收口为 shadcn 风格
- 本次不再沿用自由发挥式页面，明确按 [docs/WORKSPACE_IMPLEMENTATION_GUIDE.md](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docs\WORKSPACE_IMPLEMENTATION_GUIDE.md) 的信息架构和样式方向收口。
- 前端补齐最小可用的 shadcn 基础层：
  - [frontend/src/lib/utils.ts](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\lib\utils.ts)
  - [frontend/src/components/ui/button.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\button.tsx)
  - [frontend/src/components/ui/card.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\card.tsx)
  - [frontend/src/components/ui/badge.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\badge.tsx)
  - [frontend/src/components/ui/input.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\input.tsx)
  - [frontend/src/components/ui/textarea.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\textarea.tsx)
  - [frontend/src/components/ui/separator.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\separator.tsx)
  - [frontend/src/components/ui/scroll-area.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\scroll-area.tsx)
- 全局样式已补平台级 token，不再是零散颜色直写：
  - [frontend/src/app/globals.css](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\globals.css)
  - 增加 `background / foreground / card / primary / border / ring` 等变量；
  - 为后续页面统一 shadcn 风格提供基础。
- 工作区页已重写为文档导向的三栏工作台：
  - [frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx)
  - 左栏：工作区管理、创建、新导航；
  - 中栏：工作区总览、文档资料区、上传/录入、知识检索；
  - 右栏：AI能力面板、Skill 清单、MCP 清单；
  - 知识关系图保留为辅助视图，不再抢占主界面中心。
- 这一步的目标不是把所有页面都改完，而是先把最核心的 Workspace 页面从“功能堆叠页”改成“工作台页”，作为后续首页/Agent 页风格统一的基准。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.15 快速会话页按文档收口为 shadcn 风格
- 本阶段继续按 [docs/WORKSPACE_IMPLEMENTATION_GUIDE.md](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docs\WORKSPACE_IMPLEMENTATION_GUIDE.md) 的“快速会话页”定义收口，不再自由延展页面结构。
- 新增 shadcn 弹窗基础组件：
  - [frontend/src/components/ui/dialog.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\ui\dialog.tsx)
- 已整体重写聊天页界面层：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
- 收口后的信息架构：
  - 左侧：品牌、返回入口、新建对话、保存到工作区、对话历史、用户信息；
  - 顶部：工作区、Provider、Model、Mode、Execution、Agent Framework；
  - 中间：消息流、思考流、Tool Trace、来源展示；
  - 底部：统一 shadcn 风格输入区。
- 这一步不是单纯换皮，已把文档中要求的“保存到工作区”做成真功能：
  - 直接复用现有 `POST /api/workspace/documents/text`
  - 将当前对话整理为 Markdown 存档，写入选中工作区知识库；
  - 支持在弹窗内直接新建工作区后再保存。
- 保留并兼容现有关键能力：
  - 流式输出；
  - 思考流展示；
  - Chat / Agent Runtime 模式切换；
  - Provider / Model 选择；
  - 当前工作区偏好同步；
  - 对话历史恢复与删除。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.16 首页 Dashboard 按文档落地
- 原首页 [frontend/src/app/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\page.tsx) 只是登录后跳转页，不符合工作台文档要求。
- 已按 [docs/WORKSPACE_IMPLEMENTATION_GUIDE.md](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docs\WORKSPACE_IMPLEMENTATION_GUIDE.md) 改为真正的 Dashboard：
  - 左侧导航栏；
  - 今日课表卡片；
  - 快速会话入口；
  - 学习工作区卡片；
  - 学习状态卡片；
  - 最近对话列表。
- 实现策略：
  - 复用现有接口，不额外新造首页专用 API；
  - 使用：
    - `/api/auth/me`
    - `/api/workspace/{username}`
    - `/api/chat/conversations/{username}`
    - `/api/schedule`
    - `/api/sync-status`
    - `/api/sync-data`
- 当前首页数据策略：
  - 课表卡片先取现有 `schedule` 接口并做轻量字段兼容；
  - 数据新鲜度先根据 `sync-status.timestamp` 做绿色/黄色/红色降级展示；
  - 学习状态当前是轻量占位统计，后续再由真实学习日志替换。
- 到这一步，首页已经从“路由跳板”升级成“平台入口页”，与工作区页、聊天页的视觉风格也已基本统一到 shadcn 体系。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.17 前端页面壳层统一，旧风格页面收口
- 本轮重点不是再加零散功能，而是把仍然停留在旧样式的页面统一收口到文档要求的工作台风格。
- 新增共享工作台壳层与侧边栏基建：
  - [frontend/src/components/workspace/workbench-shell.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\workbench-shell.tsx)
  - [frontend/src/components/workspace/app-sidebar.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\app-sidebar.tsx)
- 这套壳层统一了：
  - 左侧品牌区与导航；
  - 顶部页面标题、说明、操作按钮；
  - 统计卡、区块卡、空状态卡；
  - 页面整体留白、边框、阴影和信息层次。
- 已用新壳层重写并收口的页面：
  - [frontend/src/app/skills/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\skills\page.tsx)
  - [frontend/src/app/mcp/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\mcp\page.tsx)
  - [frontend/src/app/composition/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\composition\page.tsx)
  - [frontend/src/app/settings/models/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\settings\models\page.tsx)
  - [frontend/src/app/login/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\login\page.tsx)
- 其中 MCP 页这次是结构性修复，不再靠临时缩字处理按钮溢出：
  - 导入区、探测区、流水线控制区拆分为独立卡片；
  - 操作按钮使用固定栅格布局；
  - 任务队列与流水线历史分栏展示。
- 模型设置页也已恢复为清晰可见的模型入口，明确包含：
  - Provider 选择；
  - Model 选择；
  - Base URL / API Key（当 provider 支持时）；
  - 推理模式；
  - 思考流开关。
- 登录页只改界面层，不改既有教务认证逻辑：
  - 保留验证码获取和登录流程；
  - 保留登录成功后进入 `/chat` 的行为；
  - 不再做旧式大面积渐变卡片堆叠，而是纳入统一工作台审美。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.18 教务缓存层落地，首页切换 cache-first
- 后端已补齐独立的教务缓存读取层，不再让首页直接打实时爬虫接口：
  - [backend/app/services/education_cache.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\education_cache.py)
  - [backend/app/api/education_cache.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\education_cache.py)
  - [backend/app/api/education_refresh.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\education_refresh.py)
- 新接口职责划分：
  - `/api/schedule/db`
  - `/api/grades/db`
  - `/api/user/info/db`
  - `/api/training-plan/my/db`
  - `/api/academic-progress/db`
  - `/api/exam-schedule/db`
  - `/api/execution-plan/db`
  - `/api/course-selection/db`
  - `/api/education/status`
  - `/api/refresh`
  - `/api/refresh/check`
  - `/api/refresh/progress`
- `backend/main.py` 已注册缓存读取和刷新路由。
- 同时修正了结构化存储遗漏字段，避免缓存层读到残缺数据：
  - [backend/app/services/data_processor.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\data_processor.py)
  - 现已写入 `execution_plan` 与 `course_selection`。
- 首页已切到 cache-first 读法：
  - [frontend/src/app/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\page.tsx)
  - 读取 `/api/schedule/db` 与 `/api/education/status`
  - 刷新按钮改为触发 `/api/refresh`
  - 不再使用 `/api/schedule` + `/api/sync-status` 作为首页主链路
- 这一步的意义：
  - 读缓存和触发刷新彻底解耦；
  - 首页和后续工作区页能共用同一套教务数据读取口径；
  - 后续要接完整课表页时不用再返工一遍接口。
### 31.19 工作区拆分为入口页 + 详情页
- 原先 [frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx) 承担了过多职责，既做入口页，又做详情页。
- 本轮已按文档方向拆分：
  - 入口页：[frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx)
  - 详情页：[frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
- 入口页现在只负责：
  - 列出工作区；
  - 创建工作区；
  - 跳转到具体 `workspace/[id]`。
- 详情页现在负责：
  - 三栏工作台布局；
  - 关系预览；
  - 文档录入/上传；
  - 文档清单；
  - 知识检索；
  - 右侧 AI 能力面板与建议区占位。
- 后端为此补了详情接口：
  - [backend/app/api/workspace.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\workspace.py)
  - 新增 `GET /api/workspace/{username}/detail/{workspace_id}`
- 到这一步，工作区路由已经从“单页大杂烩”变成了更可持续的入口/详情结构。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - 新增后端文件已做 `py_compile` 语法检查，通过。
### 31.20 完整课表页落地，聊天页补工作区详情入口
- 新增完整课表页：
  - [frontend/src/app/schedule/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\schedule\page.tsx)
- 当前实现口径：
  - 直接读取 `/api/schedule/db`
  - 读取 `/api/education/status` 作为缓存新鲜度标识
  - 刷新按钮走 `/api/refresh`
  - 使用稳定版周视图网格展示，不依赖实时爬虫
- 平台导航已补上“课表”入口：
  - [frontend/src/components/workspace/app-sidebar.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\app-sidebar.tsx)
- 首页课表卡片的跳转已经改为真正的完整课表页：
  - [frontend/src/app/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\page.tsx)
  - “查看 / 完整课表” 已指向 `/schedule`
  - 顶部“学习工作区”保持指向工作区，不再混淆
- 聊天页补了“打开工作区”入口：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - 当当前已选 workspace 时，可直接跳转到 `/workspace/{id}`
- 这一步的意义：
  - 文档中“完整课表页”不再停留在规划层；
  - 首页、课表页、工作区、聊天页之间已经形成更顺的跳转链；
  - 用户从首页看课表、从聊天跳工作区、从工作区回聊天，都比之前清晰。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.21 工作区显式上下文接入聊天主链路
- 本轮重点是把“工作区详情页进入聊天”从弱绑定改成显式绑定，避免聊天只依赖“上次工作区偏好”导致上下文错乱。
- 前端聊天页已修复并完成显式 `workspace_id` 接入：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
- 这次修复包含：
  - 清理了之前错误替换留下的源码损坏；
  - 正常接入 `useSearchParams`；
  - 解析 `/chat?workspace_id=xxx`；
  - 初始化时优先选中 URL 指定工作区，而不是仅依赖已保存偏好；
  - 切换工作区时同步更新 URL 与工作区偏好；
  - `/api/chat/send` 与 `/api/chat/send-stream` 都显式携带 `workspace_id`。
- 工作区详情页的聊天入口也已改成显式带参：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - 入口已从 `/chat` 改为 `/chat?workspace_id={id}`；
  - 右侧 AI 面板增加了“当前工作区上下文会被带入聊天”的提示文案。
- 后端聊天链路已补齐 `workspace_id` 透传：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - [backend/app/services/agent_runtime.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\agent_runtime.py)
- 后端行为调整：
  - `ChatRequest` 新增 `workspace_id`；
  - `_build_workspace_context(...)` 优先使用显式 `workspace_id`，没有时才回退到 session 中的 workspace preference；
  - Agent Runtime 构建运行时上下文时同样优先使用显式 `workspace_id`。
- 这一步的意义：
  - 工作区上下文不再是隐式碰运气；
  - 从工作区进入聊天时，知识检索、Agent Runtime、工作区偏好三者终于是同一套目标工作区；
  - 后续继续做工作区 AI 面板、知识库沉淀、Skill/MCP 编排时，底层上下文选择不会反复返工。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/chat.py` 与 `backend/app/services/agent_runtime.py` 已通过 `py_compile`。
### 31.22 工作区详情页从占位态升级为真实工作台
- 本轮继续围绕 [docs/WORKSPACE_IMPLEMENTATION_GUIDE.md](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docs\WORKSPACE_IMPLEMENTATION_GUIDE.md) 收口工作区详情页，不再停留在“静态占位卡片”。
- 已整体重写：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
- 这次的核心改动不是换皮，而是把右侧 AI 面板和左侧状态位接到真实平台状态：
  - 新增组合状态读取：
    - `/api/composition/{username}`
    - `/api/agents/frameworks`
  - 页面现在会同时读取：
    - 工作区详情
    - 文档清单
    - 平台 Skill 列表
    - 平台 MCP 列表
    - 当前组合编排结果
    - Agent Framework 可用性
- 页面结构变化：
  - 左栏：
    - 保留工作区核心统计；
    - 新增“运行态概览”，展示检索命中数、已启用 Skill 数、已启用 MCP 数。
  - 中栏：
    - 保留知识关系预览；
    - 保留文档录入、上传、文档清单；
    - 保留工作区内知识检索。
  - 右栏：
    - AI 上下文卡：明确展示当前工作区已绑定到聊天入口；
    - Skill Composition 卡：显示真正启用的 Skill，而不是全量注册表；
    - MCP Composition 卡：显示真正挂入当前组合的 MCP 工具；
    - Agent Frameworks 卡：展示 OpenAI Agents / LangGraph 的可用状态；
    - 建议区：改成基于当前真实工作区状态生成下一步建议，而不是固定文案；
    - Skill / MCP 清单：对比“已注册”与“已启用”状态。
- 这一步的意义：
  - 工作区详情页已经不只是“知识库壳子”，而是开始承担真正的 Agent 工作台角色；
  - 用户现在能直接看出：
    - 当前工作区有多少知识；
    - 当前编排真正启用了哪些 Skill/MCP；
    - 当前 Agent 框架能不能跑；
    - 下一步应该补知识、补 Skill，还是直接进聊天验证。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.23 中区主视图切换为知识库可视化
- 按文档最新要求，工作区详情页中区主视图不再强调图谱，而是先把“知识库本身”可视化出来。
- 新增独立组件：
  - [frontend/src/components/knowledge/KnowledgeVisualization.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\knowledge\KnowledgeVisualization.tsx)
- 组件当前实现口径不是伪装图谱，而是知识库结构视图，重点展示：
  - 文档总量
  - 关系数量
  - 高权威文档数量
  - 已就绪文档数量
  - 按文档类型分布
  - 按权威级别分布
  - 按处理状态分布
  - 高价值知识块（按 token 体量排序）
- 工作区详情页已接入该组件：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
- 同时做了语义调整：
  - “Graph Nodes” 改成 “Knowledge Units”
  - 原“知识关系预览”区块改成“知识库可视化”
  - 关系信息仍保留，但被降级为知识库结构的一部分，而不是页面主叙事
- 这一步的意义：
  - 页面重点已经从“图画得像不像”转回“知识库是否可见、可理解、可管理”
  - 后续继续做文档处理链、知识库检索、来源标注时，不需要返工页面主结构
  - 等知识库主链更稳之后，再决定是否引入更复杂的 ReactFlow 图式可视化
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
### 31.24 独立知识库 API 落地，工作区页切到知识库口径
- 本轮继续把“知识库”从工作区 CRUD 里拆出来，开始形成独立的数据读取层。
- 后端新增独立知识库 API：
  - [backend/app/api/knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\knowledge.py)
- 已注册到应用入口：
  - [backend/main.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\main.py)
- 新接口：
  - `GET /api/knowledge/{username}/{workspace_id}`
  - `GET /api/knowledge/{username}/{workspace_id}/stats`
- 同时在服务层补了统一知识库概览能力：
  - [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)
  - 新增 `get_workspace_knowledge_overview(...)`
- 当前统一输出内容包括：
  - workspace 基本信息
  - documents
  - knowledge_units
  - relations
  - ready / failed 文档数
  - total_tokens
  - by_doc_type / by_status / by_authority 分布
- 前端工作区详情页已开始切换到知识库 API 口径：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - 文档和工作区基础信息读取不再依赖旧的 `workspace/documents` 拼装，而是走 `/api/knowledge/{username}/{workspace_id}`
  - 图相关信息仍暂时从旧 detail 接口读取，后续会继续拆分
- 这一步的意义：
  - 工作区页已经开始从“工作区 CRUD 页”向“知识库工作台”迁移；
  - 后续要做知识库独立页面、聊天引用链、知识库统计卡片时，可以直接复用知识库接口；
  - 前后端口径开始围绕“知识库”统一，而不是继续混在 workspace 读接口里。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/knowledge.py`、`backend/app/services/workspace_knowledge.py`、`backend/main.py` 已通过 `py_compile`。
### 31.25 知识库来源进入聊天回答链
- 本轮开始收口“知识库有内容，但回答看不出引用了什么”的问题。
- 后端聊天链路已补来源标准化：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
- 新增能力：
  - `_format_context_source(...)`
  - `_render_workspace_rag_context(...)`
  - `_collect_context_sources(...)`
- 当前作用：
  - 工作区知识检索命中不再只是匿名文本块；
  - 会附带标准化来源，例如 `knowledge:{workspace_id}:{title}`；
  - 非流式 RAG 回答会把整理后的 `sources` 一并回给前端；
  - 流式回答结束帧也会带 `sources`；
  - 存库时 `message_meta.sources` 也会保留这些来源。
- 同时调整了工作区知识注入方式：
  - 不再只是 `标题 - 内容` 的简单拼接；
  - 现在会带：
    - 标题
    - 来源
    - 内容
  - 这样模型至少能看见知识边界和出处。
- Qwen 的 RAG 返回也做了来源收口：
  - [backend/app/services/qwen_service.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\qwen_service.py)
  - `sources` 改为只返回有效字符串来源。
- 前端聊天页引用显示已改成 badge 列表，而不是逗号拼接：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
- 这一步的意义：
  - 用户至少能看见回答引用了哪些知识源；
  - 后续继续做“点击来源回看文档”“引用片段高亮”“回答必须基于来源约束”等能力时，底层已经有来源通路；
  - 这也是减少胡编乱造的基础前提。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/chat.py`、`backend/app/services/qwen_service.py` 已通过 `py_compile`。
### 31.26 来源可回跳到工作区文档
- 本轮继续把“来源”从纯文本标签推进到“可回溯入口”。
- 后端聊天来源格式已升级为可解析结构：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - 当前知识库来源格式改为：
    - `knowledge://workspace/{workspace_id}/{title}?doc={document_id}&score={score}`
- 这样做的原因很直接：
  - 前端不需要再靠字符串猜工作区和文档；
  - 后续还能继续往 query 参数里加 chunk / source_type / authority 等字段。
- 前端聊天页已支持点击来源回跳：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - 对 `knowledge://workspace/...` 来源做解析；
  - 点击后会跳到：
    - `/workspace/{workspace_id}?doc={document_id}`
- 工作区详情页已支持目标文档高亮与滚动定位：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - 现在会读取 `?doc=` 参数；
  - 若目标文档存在：
    - 自动滚动到对应卡片；
    - 对该文档卡片加高亮边框和背景。
- 这一步的意义：
  - “回答引用来源”第一次变成了真正可交互的知识库回溯能力；
  - 用户现在可以从聊天回答直接回看对应知识文档，而不是只看一个标签；
  - 后续继续做“引用片段高亮”“按 chunk 回看”时，不需要重新设计来源协议。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/chat.py` 已通过 `py_compile`。
### 31.27 回答约束改为“有依据但不死板”
- 用户明确要求“回答不要死板”，本轮开始从提示层收口这个问题。
- 重点不是放松约束，而是把约束方式改成：
  - 继续基于证据回答；
  - 继续拒绝胡编；
  - 但不要机械重复“根据以上数据/基于以下内容”。
- 已修改：
  - [backend/app/services/qwen_service.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\qwen_service.py)
  - [backend/app/services/model_provider.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\model_provider.py)
- Qwen 侧新增统一约束模板：
  - `_grounded_answer_rules()`
- 当前规则核心：
  - 优先直接回答，语言自然；
  - 只能把给定事实当作事实；
  - 信息不足时明确说无法确认；
  - 可以概括解释，但不能把推测包装成事实；
  - 若引用知识库内容，尽量自然点明来源，而不是机械堆来源；
  - 明确禁止虚构校区、制度细节、操作入口、服务信息等。
- 两条主链都已换成新口径：
  - `chat_stream(...)` 的系统上下文注入
  - `chat_with_rag(...)` 的证据提示模板
- LiteLLM 适配器也同步做了风格收口：
  - 不再只写“严格基于数据回答”
  - 现在同样强调：
    - 自然直接
    - 无依据就明确说无法确认
    - 不补虚构细节
- 这一步的意义：
  - 现在回答风格开始从“模板式安全答复”转向“自然表达 + 证据约束”；
  - 后续继续做来源高亮和 chunk 回看时，不会再被旧的僵硬提示词拖住体验。
- 自检结果：
  - `backend/app/services/qwen_service.py`、`backend/app/services/model_provider.py` 已通过 `py_compile`。
### 31.28 引用来源推进到 chunk 级回跳
- 本轮继续把来源回溯从“文档级”推进到“片段级”。
- 后端知识库服务新增文档 chunk 读取能力：
  - [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)
  - 新增 `list_document_chunks(...)`
- 后端知识库 API 新增：
  - [backend/app/api/knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\knowledge.py)
  - `GET /api/knowledge/{username}/{workspace_id}/documents/{document_id}/chunks`
- 聊天来源协议继续升级：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - 现在知识库来源可携带：
    - `doc={document_id}`
    - `chunk={chunk_index}`
    - `score={score}`
- 前端聊天页已支持把来源跳到指定片段：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - 点击来源后会跳到：
    - `/workspace/{workspace_id}?doc={document_id}&chunk={chunk_index}`
- 工作区详情页已支持片段级定位：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - 当 URL 带 `doc` 时：
    - 自动加载该文档 chunks
    - 自动滚动到目标文档卡片
  - 当 URL 同时带 `chunk` 时：
    - 自动滚动到目标 chunk
    - 高亮该片段
- 这一步的意义：
  - 来源回溯能力已经从“回到整篇文档”进化到“回到被引用片段”；
  - 后续做“聊天中展示引用片段摘要”“点击来源展开上下文”就不再需要返工底层协议；
  - 对减少胡编也更有帮助，因为用户现在能快速反查回答到底基于哪段内容。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/knowledge.py`、`backend/app/services/workspace_knowledge.py`、`backend/app/api/chat.py` 已通过 `py_compile`。
### 31.29 聊天消息开始展示引用片段摘要
- 本轮把“引用来源”从纯 badge 继续推进成“可读片段摘要”。
- 后端聊天链路新增 `highlights`：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
- 当前 `highlights` 内容包括：
  - `source`
  - `title`
  - `snippet`
  - `document_id`
  - `chunk_index`
  - `score`
- 非流式和流式都已支持：
  - 非流式 `ChatResponse`
  - SSE `done` 结束帧
  - 持久化到 `message_meta.highlights`
- 前端聊天页已接入：
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
- 当前效果：
  - 助手消息底部除了“参考来源” badge 外，还会显示“引用片段”卡片；
  - 卡片展示：
    - 标题
    - 命中片段摘要
    - 可选 score
  - 点击片段卡片会直接跳到对应工作区 chunk。
- 这一步的意义：
  - 用户不需要先点来源再慢慢找，已经能在聊天里直接看到“本次回答到底用了哪段内容”；
  - 这对压缩胡编空间、提升可核验性，比单纯显示来源标签更有价值；
  - 后续继续做“答案中引用编号”“展开全文上下文”会更顺。
- 自检结果：
  - `pnpm --dir frontend exec tsc --noEmit` 通过。
  - `backend/app/api/chat.py` 已通过 `py_compile`。
### 31.30 平台重构进入“工作区 + 知识库 + 聊天联动”阶段
- 当前这轮升级已经不是单点修页面，而是开始形成可持续的平台骨架。
- 已经落地的主链路：
  - 工作区列表页、工作区详情页、聊天页、技能页、MCP 页、编排页、模型页、登录页都已按新工作台方向重做；
  - 前端整体风格已切到以 shadcn 风格为核心的工作台结构，不再是旧聊天页硬撑全部能力；
  - 工作区上下文已经能显式传入聊天，而不是靠隐式状态猜测；
  - 知识库读取链路已经独立出工作区 CRUD，开始形成单独的数据口径；
  - 聊天回答已经能回带来源、引用片段，并回跳到工作区文档和 chunk。
- 这一阶段的实质变化：
  - 产品形态从“校园问答页”转向“Agent 工作区”；
  - 数据形态从“零散接口拼装”转向“工作区上下文 + 知识库 + 运行配置”；
  - 交互形态从“只看答案”转向“答案可追溯、知识可回看、工作区可管理”。

### 31.31 当前已具备的基础能力边界
- 这一版已经具备“能演进的平台底座”，但还不是最终完成态。
- 当前已具备：
  - 工作区创建、浏览、详情查看；
  - 工作区知识概览读取；
  - 知识库统计可视化；
  - 聊天携带工作区上下文；
  - RAG 来源结构化返回；
  - 引用片段摘要展示；
  - 来源跳转到工作区文档；
  - chunk 级定位与高亮；
  - 回答风格从死板模板收口到“自然表达 + 不胡编”。
- 当前还没做完或还不够强的部分：
  - 回答正文里“已确认 / 推测 / 未知”的自然分层还不够稳定；
  - 工作区详情页仍有部分信息依赖旧 detail API；
  - 知识库管理还主要嵌在工作区详情里，尚未形成更完整的独立管理面；
  - chunk 交互还可以继续做成更清晰的展开、折叠、懒加载结构；
  - 图谱能力暂时降级，当前主线是知识库可视化，不是关系图炫技。

### 31.32 当前实现口径的核心原则
- 这阶段明确遵守几个约束，避免后面再次走偏：
  - 前端按 `docs/WORKSPACE_IMPLEMENTATION_GUIDE.md` 的工作台方向推进，不再自由发挥；
  - 样式以 shadcn 风格为主，强调结构清楚、层级清楚、信息面板清楚；
  - 教务系统既有成熟链路尽量少动，平台升级主要包裹在其外侧；
  - 知识库优先于图谱，能查、能看、能回溯，比“画个图”更重要；
  - 回答必须减少胡编乱造，但表达不能做成机械客服腔。

### 31.33 本阶段涉及的关键文件
- 前端主页面与骨架：
  - [frontend/src/app/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\page.tsx)
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - [frontend/src/app/workspace/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\page.tsx)
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - [frontend/src/components/workspace/workbench-shell.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\workbench-shell.tsx)
  - [frontend/src/components/workspace/app-sidebar.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\app-sidebar.tsx)
  - [frontend/src/components/knowledge/KnowledgeVisualization.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\knowledge\KnowledgeVisualization.tsx)
- 后端知识库与聊天联动：
  - [backend/app/api/knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\knowledge.py)
  - [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
  - [backend/app/services/qwen_service.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\qwen_service.py)
  - [backend/app/services/model_provider.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\model_provider.py)
  - [backend/main.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\main.py)

### 31.34 当前阶段自检结论
- 已完成的静态自检口径：
  - `pnpm --dir frontend exec tsc --noEmit` 通过；
  - 本阶段新增或重点修改的 Python 文件已通过 `py_compile`。
- 现阶段结论：
  - 平台级骨架已经成型；
  - 工作区、知识库、聊天三条主线已经开始互相打通；
  - 现在最该继续补的是“答案质量与证据表达”以及“知识库管理面的完整度”，不是继续堆零散页面。

### 31.35 下一阶段建议
- 下一阶段优先级建议如下：
  - 继续优化回答质量，让“有依据”和“自然表达”同时成立；
  - 继续增强聊天中的引用呈现，让用户更容易理解答案依据；
  - 逐步减少工作区详情页对旧接口的依赖，把知识库读取彻底收口；
  - 视文档要求决定是否拆出独立知识库管理页，而不是继续把所有能力都塞在详情页；
  - 等知识库主链稳定后，再决定是否恢复更强的图谱视图。
### 31.36 第二阶段补齐学习状态与 AI 建议/提醒后端
- 这轮开始正面补 `docs/WORKSPACE_IMPLEMENTATION_GUIDE.md` 里第二阶段还缺的三项：
  - 学习状态
  - AI 建议
  - 提醒弹窗的数据来源
- 新增学习状态服务：
  - [backend/app/services/learning_status.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\learning_status.py)
- 当前学习状态不再是前端随手乘法估算，而是基于以下真实信号聚合：
  - 当前工作区文档数 / chunk 数
  - 当前工作区对话消息数
  - 今日 / 本周提问活跃度
  - 回答中命中的来源数 / 引用片段数
  - 文档 ready / failed 状态
  - 教务缓存新鲜度
- 新增学习状态 API：
  - [backend/app/api/learning_status.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\learning_status.py)
  - `GET /api/status/{workspace_id}?username=...`
- 新增建议与提醒服务：
  - [backend/app/services/suggestions.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\suggestions.py)
- 当前建议不是静态文案，而是按以下状态生成：
  - 工作区是否为空
  - 是否存在失败文档
  - 是否缺少 Skill
  - 是否缺少 MCP
  - 是否已经有知识但还没做验证对话
  - 教务缓存是否过期
  - 是否已有考试安排可继续整理
  - 文档量是否上来了但引用命中仍偏低
- 新增建议持久化表：
  - [backend/app/models/platform.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\models\platform.py)
  - `WorkspaceSuggestion`
- 这样“接受建议 / 忽略建议”不再是假按钮，而是有实际状态。

### 31.37 第二阶段 API 与主应用注册完成
- 新增建议 API：
  - [backend/app/api/suggestions.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\suggestions.py)
- 当前接口：
  - `GET /api/suggestions/{workspace_id}?username=...`
  - `POST /api/suggestions/{workspace_id}/accept?suggestion_id=...`
  - `POST /api/suggestions/{workspace_id}/dismiss?suggestion_id=...`
  - `POST /api/suggestions/scan`
- 新路由已注册到：
  - [backend/main.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\main.py)
- 模型导出也已补齐：
  - [backend/app/models/__init__.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\models\__init__.py)

### 31.38 工作区详情页接入第二阶段真实数据
- 工作区详情页不再只展示静态建议文案。
- 已新增前端组件：
  - [frontend/src/components/workspace/learning-status.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\learning-status.tsx)
  - [frontend/src/components/workspace/reminder-modal.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\reminder-modal.tsx)
- 已接入页面：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
- 当前页面新增真实能力：
  - 左栏新增学习状态卡，不再是轻量占位统计；
  - 右栏建议区改为读取 `/api/suggestions/{workspace_id}`；
  - 建议支持“接受 / 忽略”并回写后端；
  - 高优提醒会自动弹出 `ReminderModal`；
  - 建议处理后会联动刷新建议列表和学习状态。

### 31.39 聊天链补齐 workspace 统计元数据
- 本轮顺手修了一个会影响第二阶段真实性的问题。
- 之前聊天消息虽然支持显式传 `workspace_id`，但消息持久化时没有稳定把它写入元数据，导致：
  - 工作区活跃度可能被漏算；
  - 学习状态可能失真；
  - 建议引擎会误判“有知识但没有验证对话”。
- 已修复：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
- 当前处理方式：
  - `conversation_meta.workspace_id` 会同步记录；
  - 用户消息与助手消息的 `message_meta.workspace_id` 会同步记录；
  - `learning_status` 聚合时会同时参考 conversation 级与 message 级 workspace 绑定。
- 这一步虽然不显眼，但它决定第二阶段统计是不是“真数据”。

### 31.40 本轮自检结果
- `pnpm --dir frontend exec tsc --noEmit` 通过。
- 以下 Python 文件已通过 `py_compile`：
  - `backend/app/models/platform.py`
  - `backend/app/models/__init__.py`
  - `backend/app/services/learning_status.py`
  - `backend/app/services/suggestions.py`
  - `backend/app/api/learning_status.py`
  - `backend/app/api/suggestions.py`
  - `backend/app/api/chat.py`
  - `backend/main.py`

### 31.41 第二阶段当前完成度
- 第二阶段四项里，现在状态如下：
  - 知识库可视化：已落地
  - 学习状态展示：已落地
  - AI 建议展示：已落地
  - 提醒弹窗：已落地
- 但这仍是第二阶段的第一版，不是终版。
- 还可继续增强的方向：
  - 建议接受后自动跳转到对应操作页，而不只是关闭建议；
  - 学习状态引入更细的行为日志，而不只依赖聊天和知识库信号；
  - 首页 Dashboard 也切到真实学习状态接口，而不是旧的轻量估算；
  - 提醒增加频控和已读策略，避免重复弹出。
### 31.42 独立知识库管理台落地
- 用户明确要求把重心放在知识库和 Skill/MCP/Composition，而不是继续深挖回答正文证据化。
- 本轮先把“知识库不应一直塞在工作区详情页里”这件事落地。
- 新增独立页面：
  - [frontend/src/app/knowledge/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\knowledge\page.tsx)
- 侧边导航已补知识库入口：
  - [frontend/src/components/workspace/app-sidebar.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\app-sidebar.tsx)
- 当前知识库管理台支持：
  - 按工作区切换知识库；
  - 查看文档总量、chunk 数、ready/failed 状态、token 体量；
  - 按标题/摘要搜索；
  - 按状态筛选 `all / ready / failed`；
  - 查看文档 authority / version / duplicate 提示；
  - 选中文档后查看 chunk 列表；
  - 跳回工作区详情定位文档或 chunk；
  - 直接跳去聊天页做工作区验证；
  - 删除知识文档。
- 这一步的意义：
  - 知识库开始从“工作区详情的一个区块”升级成“独立管理对象”；
  - 后续做批量治理、失败修复、版本链处理时，不需要再把工作区详情页挤爆。

### 31.43 知识库 API 补齐删除能力
- 为支持独立知识库管理台，本轮补了文档删除接口。
- 服务层新增：
  - [backend/app/services/workspace_knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\services\workspace_knowledge.py)
  - 新增 `delete_document(...)`
- API 新增：
  - [backend/app/api/knowledge.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\knowledge.py)
  - `DELETE /api/knowledge/{username}/{workspace_id}/documents/{document_id}`
- 当前删除逻辑：
  - 删除文档记录；
  - 若来源已无其他文档引用，则同步删除 source；
  - 尝试删除本地存储文件；
  - 保持知识库接口口径统一，不再让知识库页去走 workspace CRUD 风格接口。

### 31.44 Composition 页从“开关列表”升级到联动面板
- 之前的组合页本质上只是 Skill/MCP 启停开关，不足以支撑“拼积木平台”的感知。
- 本轮增强：
  - [frontend/src/app/composition/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\composition\page.tsx)
- 新增内容：
  - 运行态摘要：
    - 当前真正启用的 Skill 数
    - 当前真正启用的 MCP 数
    - 已启用对象名的快速预览
  - 联动建议区：
    - 根据当前是否启用 Skill/MCP，给出下一步建议；
    - 直接连到知识库管理台和聊天验证入口。
  - Skill 拼装区增强：
    - 不再只显示名字；
    - 补显示 Skill 描述；
    - 补显示 `input_schema`，让用户知道这个 Skill 接收什么输入结构。
  - MCP 拼装区增强：
    - 补显示 MCP 描述；
    - 补显示 `tool_schema.parameters`，让用户知道工具参数长什么样；
    - 继续保留启停与顺序调整。
- 这一步的意义：
  - Composition 页开始具备“平台编排台”味道，而不只是配置列表；
  - 用户第一次能更直接地理解：
    - 自己启用了哪些能力；
    - 这些能力需要什么输入；
    - 下一步应该去知识库、聊天还是继续补工具。

### 31.45 当前阶段判断更新
- 用户本轮关心的两项状态：
  - 知识库独立管理台：已进入第一版可用态
  - Skill/MCP/Composition 深度联动：已从“开关页”进入“联动面板第一版”
- 但还没到终态：
  - 知识库管理台目前还没有批量操作、失败重试、版本链折叠；
  - Composition 目前还是“可见 schema + 可见对象 + 可跳验证”，还没有真正做到基于 schema 的编排模拟或参数预填；
  - 工作区内嵌 AI 面板还未完全替代跳转式聊天入口。

### 31.46 本轮自检结果
- `pnpm --dir frontend exec tsc --noEmit` 通过。
- `backend/app/services/workspace_knowledge.py`、`backend/app/api/knowledge.py` 已通过 `py_compile`。
### 31.47 工作区内嵌 AI 面板基础版落地
- 用户明确要求“不要陷入局部最优化陷阱，先做好基础”，这一轮没有继续深挖回答正文证据化，而是补平台基础能力：工作区内嵌 AI 面板。
- 新增组件：
  - [frontend/src/components/workspace/ai-panel.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\ai-panel.tsx)
- 已接入工作区详情页：
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
- 当前基础版能力：
  - 在工作区右栏直接发起对话，不再只能跳去 `/chat`；
  - 自动携带当前 `workspace_id`；
  - 左侧显示“当前工作区自己的会话列表”；
  - 支持切换工作区会话并加载历史；
  - 复用现有 `/api/chat/send-stream` 流式链路；
  - 复用现有来源回跳协议，引用片段仍可跳回工作区文档 / chunk；
  - 复用 `tool_trace` 展示，不另起新协议。
- 这一步的意义：
  - 工作区详情页第一次真正具备“内嵌式 AI 工作流”，而不是按钮跳转页；
  - 后续继续做深度联动时，不需要再先搭聊天壳子。

### 31.48 对话列表补 workspace 维度过滤
- 为了让工作区内嵌 AI 面板不是“把全部对话都塞进来”，本轮顺手补了会话列表的 workspace 过滤能力。
- 修改：
  - [backend/app/api/chat.py](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\backend\app\api\chat.py)
- 当前新增能力：
  - `GET /api/chat/conversations/{username}?workspace_id=...`
  - 返回项新增 `workspace_id`
- 这样内嵌 AI 面板能只显示当前工作区对应的对话，不会把别的工作区上下文混进来。

### 31.49 当前阶段基础能力判断
- 到这一轮为止，几个重要基础项状态如下：
  - 知识库独立管理台：第一版可用
  - Skill/MCP/Composition 联动面板：第一版可用
  - 工作区内嵌 AI 面板：基础版已落地
- 这说明平台底座已经开始从“分散页面”收口为“工作区内闭环”：
  - 知识管理
  - 能力编排
  - AI 验证
  - 来源回溯
- 但当前仍是基础版，不是最终版：
  - 内嵌 AI 面板还没有模型/模式/执行框架全配置；
  - 也还没有文件附件、保存对话到知识库等高级动作；
  - 目前目标是先把工作区闭环打通，不先堆功能密度。

### 31.50 本轮自检结果
- `pnpm --dir frontend exec tsc --noEmit` 通过。
- `backend/app/api/chat.py` 已通过 `py_compile`。
### 31.51 内嵌 AI 面板补最小可配能力
- 为了避免工作区内嵌 AI 面板继续停留在“固定参数黑盒”，本轮补了最小可配置项。
- 修改：
  - [frontend/src/components/workspace/ai-panel.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\components\workspace\ai-panel.tsx)
- 当前新增配置：
  - `execution_mode`: `chat / agent`
  - `agent_framework`: `openai_agents / langgraph`
  - `show_thinking`: `on / off`
- 当前原则：
  - 先补最关键的运行模式配置，不一次性把完整聊天页所有模型设置全塞进来；
  - 避免基础版面板过早复杂化；
  - 后续如需要，再补 provider / model / api_base / api_key 等完整配置。
- 这一步的意义：
  - 工作区内嵌 AI 面板已经从“只能发消息”升级成“可以切运行模式做验证”；
  - 现在它更适合承担“工作区内联调面板”角色。

### 31.52 第一个可用版本的判断口径
- 到这一轮为止，平台已经具备第一次做端到端测试的基础。
- 当前建议定义的“第一个可用版本”范围：
  - 工作区可创建；
  - 工作区详情页可进入；
  - 文本 / 文件可入知识库；
  - 独立知识库管理台可查看 / 删除文档；
  - Composition 可启停 Skill / MCP；
  - 工作区内嵌 AI 面板可直接发起带 `workspace_id` 的对话；
  - 回答中的引用可回跳文档 / chunk；
  - 学习状态 / 建议 / 提醒可正常显示。
- 也就是说：
  - 它还不是“功能全满版”；
  - 但已经快到“第一个可用版本”门槛，而不是只剩 UI 拼装。

### 31.53 本轮自检结果
- `pnpm --dir frontend exec tsc --noEmit` 通过。
- `backend/app/api/chat.py` 已通过 `py_compile`。

### 31.54 进入“第一个可用版本测试前收口”阶段
- 当前阶段正式从“继续扩平台能力”切到“第一个可用版本测试前收口”。
- 这一步的目标不是继续堆新能力，而是把已经落地的工作区闭环收紧，确保第一次 Linux 端到端测试有明确边界。
- 本轮新增文档：
  - [docs/MVP_TEST_GUIDE.md](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\docs\MVP_TEST_GUIDE.md)
- 文档里已经固定了三件事：
  - MVP 这次到底测什么，不再边测边改口径；
  - Linux 侧何时必须 `--build`，何时只需 `restart`；
  - 测试顺序、通过标准、问题记录格式。
- 当前被纳入第一个可用版本的闭环能力：
  - 登录后进入首页 / 工作区；
  - 创建工作区并进入详情页；
  - 文本 / 文件写入知识库；
  - 独立知识库管理台查看文档、chunk、删除文档；
  - Skill 导入；
  - MCP 导入；
  - Composition 启停 Skill / MCP 并调整 MCP 顺序；
  - 工作区内嵌 AI 面板发起带 `workspace_id` 的对话；
  - 回答来源跳回工作区文档 / chunk。
- 当前明确不作为这次 MVP 阻塞项的内容：
  - 回答正文内部更深的证据化分层；
  - 拖拽式编排；
  - 多步 Agent orchestration；
  - 更重的模型配置迁移。
- 当前判断：
  - Skill / MCP 导入与“拼积木”已经进入第一版可用态；
  - 现在最应该做的是按测试清单跑一轮，而不是再扩新页面。

### 31.55 前端按测试结论做第一轮结构收口
- 用户提供了新的前端测试结论，明确要求先处理结构问题，不要继续堆“废话区块”。
- 本轮已收口的页面：
  - [frontend/src/app/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\page.tsx)
  - [frontend/src/app/schedule/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\schedule\page.tsx)
  - [frontend/src/app/chat/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\chat\page.tsx)
  - [frontend/src/app/knowledge/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\knowledge\page.tsx)
  - [frontend/src/app/workspace/[id]/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\workspace\[id]\page.tsx)
  - [frontend/src/app/composition/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\composition\page.tsx)
  - [frontend/src/app/settings/models/page.tsx](C:\Users\ASUS\Desktop\智能体构建\project_20260404_235124\frontend\src\app\settings\models\page.tsx)

- 首页收口：
  - 删除“最近对话”整块；
  - 学习状态并入工作区区块；
  - 学习工作区卡片扩大，不再被零散面板切碎。

- 课表页收口：
  - 去掉独立“课表状态”大区块；
  - 周视图课表成为唯一主视图；
  - 顶部直接显示上一周 / 下一周和第几周；
  - 单元格体积缩小，减少大块空白。

- 快速会话页收口：
  - 左栏只保留：
    - 新建对话
    - 保存到工作区
    - 对话历史
  - 去掉工作区/Skill/MCP/模型设置等侧栏入口；
  - 去掉顶部默认工作区、skills、mcp、agent 等一排摘要标签；
  - 输入框下方只保留：
    - 模型名称选择
    - 思考模式开关
  - 聊天页仍保留流式输出、Tool Trace、引用回跳。

- 工作区详情页收口：
  - “工作区状态 + 学习状态”合并到 `WORKSPACE DETAIL`；
  - 删除：
    - 运行态概览
    - 平台接入能力
    - 知识检索区
    - 右栏 Skill/MCP/Framework 清单
  - 左边保留：
    - 知识库可视化
    - 极简文档资料区（输入文本 / 选文件 / 入库整理）
  - 右边只保留 AI 对话面板；
  - 建议继续由提醒弹窗承接，不再单独占一个建议面板。

- 知识库页收口：
  - 增加“快速入库”区；
  - 支持直接输入文本或上传文件；
  - “入库整理”保持极简入口；
  - 文档管理和 chunk 预览继续保留给治理使用。

- 模型设置页增强：
  - 从“简单 provider 选择”改成“模型导入”口径；
  - `litellm` 路径现在支持填写：
    - 自定义模型名
    - API Base URL
    - API Key
  - 这一步已经接近用户要求的“像 openclaw / cc 那种自定义接入基础形态”。

- 编排页联动增强：
  - 组合编排页新增工作区绑定；
  - 可直接选择当前验证工作区；
  - 可跳转到该工作区的知识库和聊天/工作区验证。

- 本轮自检：
  - `pnpm --dir frontend exec tsc --noEmit` 通过；
  - `backend/app/api/models.py`、`backend/app/services/model_provider.py` 已通过 `py_compile`。

- 当前阶段判断：
  - 前端已经从“功能很多但版面混乱”进入“第一轮结构收口完成”；
  - 还没做的重点不再是继续堆页面，而是做一轮 Linux 实测，把真实交互问题收回来。
